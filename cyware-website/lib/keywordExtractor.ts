export async function extractKeywords(content: string): Promise<string[]> {
    // For now, just return dummy keywords.
    // Replace this with real logic or API calls if needed.
    return ['testing', 'automation', 'threat intelligence', 'SOAR']
  }